1. Case....when
SELECT x, y, z,
CASE WHEN x + y > z AND x + z > y AND y + z > x THEN 'Yes'
    ELSE 'No'
END AS 'Triangle'
FROM triangle


2. IF
select *, 
if(x+y>z and x+z>y and y+z>x, 'Yes','No') as triangle
from triangle
