select s.id, s.name
from students s
left join departments d on s.department_id=d.id
where d.id is null
---------------------------
select students.id, students.name from
Students left join Departments on Departments.id = Students.department_id
where departments.id is null
